# Django Settings
SECRET_KEY=your-secret-key-here-change-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database
DB_NAME=ecoalerta
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432

# Redis (opcional - si no está disponible, se usa caché en memoria)
REDIS_URL=redis://127.0.0.1:6379/1

# Azure (para producción)
AZURE_FRONTEND_URL=https://tu-frontend.azurestaticapps.net

# ML Predictions (opcional)
ENABLE_LOCAL_PREDICTIONS=false
TRAIN_PREDICTIONS_ON_START=false
TRAIN_PREDICTIONS_MIN_SAMPLES=30

# SQLite local (para desarrollo sin PostgreSQL)
USE_SQLITE_LOCAL=false

