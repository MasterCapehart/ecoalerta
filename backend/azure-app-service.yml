# Configuración para Azure App Service
# Este archivo define cómo Azure debe construir y desplegar la aplicación

version: 1.0.0

build:
  # Comando para instalar dependencias del sistema
  preBuild: |
    apt-get update && apt-get install -y libpq-dev gdal-bin libgdal-dev python3-gdal
  
  # Comando para instalar dependencias de Python
  build: |
    pip install -r requirements.txt
  
  # Comando para ejecutar migraciones y recopilar archivos estáticos
  postBuild: |
    python manage.py collectstatic --noinput
    python manage.py migrate --noinput

startup:
  # Script de inicio
  command: bash startup.sh

